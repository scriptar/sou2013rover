<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.sou.rover2013"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:name="edu.sou.rover2013.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.FirstActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ConnectionActivity"
            android:label="@string/title_activity_screen__bluetooth" >
        </activity>
        <activity
            android:name=".activities.TelemetryActivity"
            android:label="@string/title_activity_screen_telemetry" >
        </activity>
        <activity
            android:name=".activities.ModeSelectActivity"
            android:label="@string/title_activity_screen_mode_select" >
        </activity>
        <activity
            android:name=".activities.ControlSimpleActivity"
            android:label="@string/title_activity_control_simple" >
        </activity>
        <activity
            android:name=".activities.WiFiServerActivity"
            android:label="@string/title_activity_wi_fi_server" >
        </activity>
    </application>

</manifest>